import { devKits as libraries } from './libraries';
import { devKits as ui } from './ui';
import { devKits as icons } from './icons';
import { devKits as fonts } from './fonts';
import { devKits as tools } from './tools';

export const allDevKits = [...fonts, ...icons, ...libraries, ...ui, ...tools];